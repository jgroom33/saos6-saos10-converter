Value Required destination (\S+)
Value gateway (\S+)
Value metric (\S+)
Value egress-interface (\S+)

#
#ip route add destination 10.254.250.252/30 gateway 10.254.250.249

Start
  ^ip route add destination ${destination}(?=.* gateway ${gateway})?(?=.* egress-interface ${egress-interface})?(?=.* metric ${metric})? -> Record

EOF