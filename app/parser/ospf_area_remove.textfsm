Value Required area_ip (\S+)
Value ospf_instance (\S+)
Value prefix_filter (\S+)
Value range (\S+)

#ospf area remove area-ip <ip> ospf-instance foo prefix-filter <filter> range <range_value>

Start
  ^#ospf area remove area-ip ${area_ip}(?=.* ospf-instance ${ospf_instance})?(?=.* prefix-filter ${prefix_filter})?(?=.* range ${range})?-> Record

EOF
